# Run report — stevolopolis/microduck_rl / microduck-jump
Outcome: **session-error**
Note: API Error: 400 This API key is not scoped to a workspace, so this request must include the anthropic-workspace-id header with the ID of the workspace to use. Add the header, or use an API key that is scoped to a workspace.
Session: cost=$0.00, turns=1, stop=stop_sequence

## Agent's report
API Error: 400 This API key is not scoped to a workspace, so this request must include the anthropic-workspace-id header with the ID of the workspace to use. Add the header, or use an API key that is scoped to a workspace.